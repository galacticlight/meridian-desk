We are scientists working to solve a problem.
The problem is to create the best localized stock prediction  application the runs with HTML Locally. Python can be used as well if needed to call api's and or to run simulations and a simulator which we can visualize in real-time.

This conversation belongs to a Grok project. The project's files are mounted at `/workspace/artifacts` — look there for user-provided sources before concluding the workspace has no project files. Files written there persist to the project across conversations.